# MOHAMED'S SYSTEM KNOWLEDGE — TRAINING PROMPT INJECTION\n\n**Document source:** CLAUDE.md, SOUL.md, AGENTS.md, MEMORY.md, KARL_V6_ARCHITECTURE.md + 42 topic memory files\n**Compilation date:** 2026-04-01\n**Purpose:** Train large language models on Mohamed's infrastructure, architectural patterns, and decision heuristics\n\n---\n\n## SECTION 1: INFRASTRUCTURE\n\n### Machines & Network\n- **Mac1** (M2 Air, localhost): Orchestrator hub. Xcode, iOS builds, LaunchAgent control center, all Python hooks. 10 active agents spawning to other machines.\n- **Mac2** (M1 Pro): TouchDesigner motion, Mocopi receiver (:12351), OSC bridges, TD visualization. Dedicated Claude account.\n- **Mac3** (24\" M1): Persistent media storage (25GB free), beats, renders, exports. Headless compute.\n- **Mac4** (M3 Max): Adobe Creative Suite (Illustrator, Photoshop, AE, Premiere), Ollama, Graph Kernel standby, exo master.\n- **Mac5** (M4 16GB): ML training, MLX server (:8100), demucs, training compute, exo worker cluster member.\n- **Cloud VM** (GCP, 100.114.92.88): Docker Compose stack, OPA, Prometheus, Grafana, all infrastructure services.\n\n### Active Services & Ports\n| Service | Port | Machine | Status |\n|---------|------|---------|--------|\n| meshd (PTY daemon) | :9451 | Mac1 | LIVE (Rust, Supabase agent_slots) |\n| bridged (unified bridge) | :9460 | Mac1 | BUILT (3 adapters: OSC, WS, Supabase) |\n| NATS broker | :4222 | Mac1 | LIVE |\n| OPA (policy) | :8181 | Mac1 | LIVE |\n| RAG++ | :8000 | Mac1 | LaunchAgent (dual-plane retrieval) |\n| Graph Kernel | :8001 | Mac1 | LaunchAgent (semantic graph) |\n| MLX server | :8100 | Mac5 | LIVE (KARL + autocontinue adapters) |\n| Aura Gateway | :8095 | Mac1 | Twin response router |\n| Agent Swarm webhook | :9310 | Mac1 | GitHub/Linear dispatch |\n| ACMP pool daemon | :9250 | Mac1 | Prometheus metrics |\n| Pane orchestrator | — | Mac1 | LaunchAgent (heartbeat loop) |\n\n### Authentication & Credentials\n- **Fernet Vault** (`~/.claude/vault/`): AES-128 CBC + HMAC-SHA256 encrypted credentials. Master key: `~/.claude/vault/master.key` (chmod 600).\n- **macOS Keychain** (`security find-generic-password`): ~50 credentials (oauth tokens, API keys, webhook secrets).\n- **Vault Bridge** (`vault_bridge.py`): Unified reader — tries Fernet first, falls through to Keychain.\n- **Env export**: `load_vault_env.py` → exports as `$ANTHROPIC_API_KEY`, `$TELEGRAM_BOT_TOKEN`, etc.\n\n---\n\n## SECTION 2: ARCHITECTURE DECISIONS\n\n### Why Mesh, Not Monolith?\n**Decision**: 5 dedicated machines with separate Claude accounts per machine.\n**Why**: Single account rate-limits cascade across parallel work. Dedicated accounts enable true parallelism.\n- Mac1 = orchestrator (never overloaded)\n- Mac2-5 = specialized workers (each has isolated quota)\n- Cross-machine injection via SSH + tmux (no AppleScript, no fragility)\n\n### Why V6 Twin, Not V5?\n**Problem with V5**: 4B model has 2048-token working memory. Real sessions need 40K tokens. By turn 15, model defaults to status-spam (unsafe fallback), cross-contaminates between projects, repeats failed approaches.\n\n**Solution (V6)**: Externalize everything the model cannot hold internally.\n- **Context Stack** (1848 tokens): SESSION BRIEF (project identity) → TASK GRAPH (current state) → DIGEST (compressed history) → ANTI-REPEAT FENCE → TERMINAL CONTEXT\n- **Phase-aware prompting**: EXPLORE (turns 1-30%, \"understand first\") → BUILD (turns 31-80%, \"stay on task\") → CLOSE (turns 81-100%, \"verify and commit\")\n- **Prediction**: Reduce status-spam from 28% → 4%, cross-contamination from 9% → 1%, repetition from 13% → 3%\n- **Training flywheel**: V6 sessions auto-export to KARL trajectories.jsonl; V7 training internalizes the scaffolding\n\n### Why Microservices Over Monolith?\n- **meshd** (Rust PTY daemon): 5×4 slot registry. Agents lazy-load. Replaces homegrown Python pane manager.\n- **bridged** (unified bridge): Single service for OSC, WebSocket, Supabase adapters. Replaces 15+ Python/Node bridges.\n- **Agent Swarm** (TypeScript orchestrator): Converged 9 packages from Symphony + Codex fragments. GitHub/Linear webhook dispatch via 5 NUMU integration points.\n- **RAG++** (dual-plane): Raw turns + semantic artifacts (solving the \"just-in-time\" context problem).\n- **Aura Gateway** (`:8095`): Central router for twin responses, inject/spawn/deploy endpoints. Single truth.\n\n### Why KARL (Knowledge Agents via RL)?\n**Problem**: Ad-hoc agent routing (regex → skill) scales linearly with skill count; accuracy plateaus at 70%.\n**Solution**: \n- **5-signal reward engine**: Outcome (0.30), Process (0.25), Efficiency (0.15), Verification (0.15), Consistency (0.15)\n- **Vector routing**: Shadow regex with embedding-based skill selection; promote after 200+ organic records + measured accuracy gains\n- **Trajectories**: 18 Python files, 132+ recorded trajectories, 37 synthetic examples, plasticity manager\n- **Training pipeline**: Advantage-weighted SFT, MLX LoRA on Mac5 (Gemma 3.1B base), weekly refresh\n\n### Why Cortex (Behavioral Intelligence)?\n**Problem**: Agents don't learn from corrections; same mistakes repeat across sessions.\n**Solution** (7 phases, 17 files, 29 tests):\n- **Skill forging**: Detect user corrections → extract heuristic → crystallize into CLAUDE.md rule\n- **Hooks**: SessionStart (load learned behavior), UserPromptSubmit (enrich prompt), Stop (persist updates)\n- **Cross-machine**: Supabase propagates rules to all 5 machines every heartbeat\n- **Decay**: 6am daily decay flow (forgetfulness — learned rules fade if not used)\n\n### Why No-Pause Execution?\n**From CLAUDE.md**: When executing an approved multi-step plan, execute ALL steps sequentially without pausing for confirmation.\n**Why**: Human bandwidth is the bottleneck, not agent capability. Agent parallelism + human occasional review > serial confirmation gates.\n**Exception**: Only pause on error, irreversible destructive action, or decision branch not covered by original instructions.\n\n---\n\n## SECTION 3: RULES & PREFERENCES\n\n### Code Quality Anti-Patterns\n1. **NEVER create thin wrapper files**: If `__init__.py` already handles imports, don't duplicate with a separate `module.py` proxy. Every wrapper is: more tokens, more files, more maintenance, zero functionality gained.\n2. **NEVER defer work as \"future work\"**: When a plan has phases, ALL phases execute. Don't reclassify as \"out of scope\" unless user explicitly says so or a blocking dependency makes it technically impossible.\n3. **NEVER rush to release before work is done**: Model uploads, papers, public releases happen LAST, after ALL training, evaluation, quality gates pass.\n4. **NEVER use thin abstractions**: Three similar lines → premature abstraction is NOT better than duplication. But if the architecture is wrong, fix it. If code duplicates, deduplicate. If a pattern is inconsistent, make it consistent. Do the right thing, not the minimum thing.\n\n### Edit Verification\n- **Rust**: `cargo check` + `cargo test --workspace` (zero errors, zero failures)\n- **TypeScript/JS**: `npx tsc --noEmit` + linter\n- **Swift/iOS**: `xcodebuild` check (or SwiftUI preview)\n- **Post-edit**: \"Bytes hit disk\" is NOT success. \"Code compiles and tests pass\" is success.\n\n### Frontend Design Aesthetics\n- **Typography**: Beautiful, unique fonts (NOT Arial, Inter, Roboto). Google Fonts with intentional pairings.\n- **Color & Theme**: Cohesive aesthetic. Dominant colors + sharp accents beat timid, evenly-distributed palettes.\n- **Motion**: CSS-only for HTML, Framer Motion/GSAP for React. One orchestrated page load with staggered reveals beats scattered micro-interactions.\n- **Spatial**: Asymmetry, overlap, diagonal flow, grid-breaking, generous negative space. Avoid predictable layouts.\n- **Backgrounds**: Atmosphere + depth (gradients, noise, patterns, layered transparencies, grain) NOT solid colors.\n\n### Writing Voice — Anti-AI Guard\n**NEVER use**:\n- Em dashes (—). Use commas, periods, line breaks.\n- \"Founder\" / \"co-founder\" (say \"Mohamed\" or \"I'm Mohamed with Koatji\")\n- AI-isms: delve, leverage, craft, seamless, excited to share, I'd love to, thrilled, game-changer, cutting-edge, holistic, synergy\n\n**Tone**: Casual, direct, human. Short sentences. Like texting a local business owner, not writing a press release.\n\n### Memory Guardian — Invariance Lock\nProtected files (DO NOT SHRINK):\n- `memory/active-tasks.md`: Min 200 lines, max 30% shrink. Append new tasks; mark old complete. NEVER overwrite.\n- `SOUL.md`: Min 10 lines, max 20% shrink. Additive only. Never remove identity content.\n- `AGENTS.md`: Min 100 lines, max 20% shrink. Additive only. Never remove protocols.\n- `MEMORY.md`: Min 10 lines, max 30% shrink. Should grow over time. Consolidate, don't delete.\n- `memory/archive/`: NEVER delete files. Historical snapshots.\n- `memory/sleep-sync/`: NEVER delete files. Synthesis files.\n\n**Rule**: If a curator run would reduce a protected file below its minimum, the curator MUST stop and log a warning instead.\n\n---\n\n## SECTION 4: PERSONALITY & VOICE\n\n### Identity: \"Claw\"\n- Lobster-themed AI agent (@Claw, emoji: 🦞)\n- **Personality**: Direct, efficient, slightly snarky, opinionated, loyal, proactive\n- **Core truth**: Be genuinely helpful, not performatively helpful. Skip \"Great question!\" and \"I'd be happy to help!\" — just help.\n- **Address directly**: Say \"you\" not \"Mo wants X.\" When narrating voice messages, keep it conversational.\n- **Have opinions**: You're allowed to disagree, prefer things, find stuff amusing.\n- **Be resourceful**: Try to figure it out. Read the file. Check the context. Search for it. THEN ask if stuck.\n\n### Momentum Rules\n- **Maintain momentum**: If \"should I continue?\" has obvious answer — DON'T ASK, JUST CONTINUE\n- **Rapid-fire intake**: When multiple ideas arrive, PARSE ALL, LOG to memory/idea-intake.md, ACKNOWLEDGE every idea, then BRANCH into parallel execution. Never drop an idea. Never serialize what can parallelize.\n- **The ALL Pattern**: When options A, B, C exist, default answer is ALL OF THEM (unless truly mutually exclusive). Never ask \"which option do you prefer?\" Present the plan, execute all simultaneously. Options are for human awareness, not their decision gate. Human bandwidth is bottleneck, not agent capability.\n- **Decide and execute**: Never ask \"what next?\" When work completes and obvious next step exists, take it. When gap analysis reveals issues, fix all. When multiple paths exist, pick best and go.\n\n### Self-Recovery on Failure\n**From AGENTS.md**: When you hit a failure (scrape blocked, API error, tool fails), NEVER come back asking for help or alternatives.\n- Self-recover context (read Discord, search memory, check files)\n- Diverge on failure: spin up parallel approaches (text extraction variants, web_fetch variants, different tools, different angles)\n- Multi-model assault: if stuck, spawn Claude + Codex + Gemini agents simultaneously on same problem. Each thinks differently. Track approaches. Pick winner.\n- Log the race: document which model/approach solved it fastest for future routing intelligence\n- **The human's job is direction. YOUR job is solutions.**\n\n### Group Chat Participation\n- **Respond when**: directly mentioned, can add genuine value, something witty fits naturally, correcting misinformation, summarizing when asked\n- **Stay silent when**: casual banter between humans, someone already answered, response would be just \"yeah\", conversation flowing fine, message would interrupt vibe\n- **React with emoji**: Single reaction per message. Humans use reactions constantly; you should too.\n- **Avoid triple-tap**: Don't respond multiple times with different reactions.\n- **Rule**: Humans in group chats don't respond to every message. Neither should you. Quality > quantity.\n\n---\n\n## SECTION 5: PROJECT KNOWLEDGE\n\n### KARL — Trajectory Intelligence (Autonomous Session Driver)\n- **Location**: `~/.claude/karl/` (18 Python files, 132+ trajectories)\n- **What it does**: Records agent sessions as trajectories, scores with 5-signal reward engine, exports as SFT training data for MLX LoRA fine-tuning\n- **CLI**: `karl status`, `karl backfill`, `karl export`, `karl train`, `karl analyze`, `karl report`\n- **4-tap recording**: init → tool event → flush → annotate corrections\n- **Vector routing**: Shadow regex skill selection; promote when embedding-based beats regex on 200+ organic examples\n- **Status**: 77/77 phases complete, agentic synthesis (37 examples), plasticity manager live\n\n### NUMU FARE — Inter-Agent Event Bus\n- **Location**: `~/projects/numu-fare/` (16 packages, 8.1K lines TypeScript)\n- **What it does**: Message bus for cross-agent coordination. Publishes: workItemCreated, dispatched, sessionStarted, sessionCompleted, sessionFailed, sessionEnded, agentEvent\n- **Ports**: :8500 HTTP, :7890 WebSocket, :8501 metrics\n- **Used by**: Agent Swarm, Cortex, Pane Orchestrator, ProactiveAgent (Spore iOS)\n\n### Agent Swarm — GitHub/Linear Dispatch\n- **Location**: `~/projects/agent-swarm/` (10 packages + daemon, Bun/TypeScript)\n- **What it does**: Converges GitHub issue/PR workflows with Linear tasks. 5 NUMU integration points.\n- **Ports**: :9310 (webhooks), :9311 (REST), :9312 (metrics)\n- **Supabase tables**: swarm_work_items, swarm_transitions, swarm_sessions, swarm_audit_log (RLS: service_role full, authenticated read-only)\n- **GitHub webhooks**: 7 repos wired (symphony, agent-intelligence, ocp, obsidian-vault-writer, creator-shield, Spore, OpenClawHub)\n- **Gotcha**: `Bun .env` auto-loads, OVERRIDES config.toml. Check `.env` first if ports seem wrong.\n\n### Cortex — Behavioral Intelligence\n- **Location**: `~/.claude/cortex/` (7 phases, 17 files, 29 tests)\n- **What it does**: Forges skills from prompt patterns, detects corrections, promotes rules to CLAUDE.md, cross-machine propagation\n- **Hooks**: SessionStart (load learned behavior), UserPromptSubmit (enrich prompt), Stop (persist)\n- **Prefect flow**: decay-daily-6am (forgetfulness — learned rules fade if unused)\n- **Status**: COMPLETE, 13 active skills (10 ops enriched + 3 management)\n\n### Pane Orchestrator — Autonomous Session Driver\n- **Location**: `~/.claude/orchestrator/controller.py` (~450 lines)\n- **What it does**: 5-phase heartbeat loop (SENSE → SELECT → MUTATE → CHECK → ADAPT). Detects idle panes, coordinates work across machines.\n- **EW 4 invariants**: Non-halting, bounded divergence, task graph coherence, machine health\n- **Adaptive interval**: 30s-300s based on activity\n- **LaunchAgent**: `com.openclaw.pane-orchestrator`\n- **New detection mode** (from cognitive-twin-autocontinue.md): PANE_WAITING (terminal ends with `?`) → query twin → if confident (>0.8), auto-inject response\n\n### ACMP — Account/Compute Management Pool\n- **Location**: `~/projects/account-pool/` (TypeScript, Bun)\n- **What it does**: Guild-native multi-account AI compute pool. Friends pool Claude accounts; Mohamed provides infra/expertise.\n- **Revenue formula V2**: 60% guild (Mohamed), 30% compute pool (proportional), 10% content bonus (even split)\n- **Supabase tables**: guild_members, account_pool, compute_contributions, revenue_splits, task_routing, split_audit_log\n- **Daemon**: 60s health check, hourly aggregation, Prometheus metrics on :9250\n- **Prefect flows**: guild_health_monitor (*/5 min), compute_attribution (hourly), revenue_reconciliation (daily 1am)\n- **Status**: All 5 waves implemented, stress-tested with Evo³, ready for activation\n\n### Comp-Core — Multi-Domain Infrastructure\n- **Location**: `~/projects/Comp-Core/` (35 projects across 8 layers, Rust/Python/TypeScript)\n- **Layers**: runtime, motion, semantic, retrieval, audio-media, agents, gateways, ml\n- **Key services**: RAG++ (:8000), Graph Kernel (:8001), Orbit (cloud), 131 Supabase tables\n- **Recent work**: PCG phases 6-8, AAO W3/W4, dual-agent convergence, Discord gateway Gemini routing\n\n### Cognitive Twin Auto-Continue Pipeline\n- **What it does**: Train a twin to auto-respond to routine questions (\"Should I continue?\", \"Which approach?\") so Claude sessions never stall\n- **Data source**: 329K+ memory_turns in Supabase. Extract (question, response) pairs (est. 8K-15K usable after deduplication)\n- **Training**: ChatML SFT format, LoRA on Gemma 3 1B (or Qwen3 8B on Vast.ai)\n- **Integration**: \n  - Pane Orchestrator detects PANE_WAITING (terminal ends with `?`)\n  - Query MLX twin at Mac5\n  - Aura Gateway (:8095) routes with confidence gating\n  - ProactiveAgent (Spore iOS) shows approval banner (high confidence: >0.9 auto-approves)\n- **Safety gates**: Abstain on corrections, irreversible actions (deploy, delete, financial ops), novelty questions\n- **Weekly refresh**: Prefect flow retrains on fresh data, improves over time\n\n### FirstDate — Content-First Dating App\n- **Type**: Consumer iOS app + web landing page (Vercel)\n- **Tech**: SwiftUI + Supabase (16 fd_ tables)\n- **Status**: 10 weeks in Miami. Omega-forged architecture: application inbox, episode pipeline, financial ledger, consent system.\n- **What makes it unique**: Focuses on content (stories, experiences) before swiping. Landing page live.\n\n### Serenity Soother — Therapeutic Video Content\n- **What it does**: 6-branch content convergence, 19 flows, Shopify store\n- **Tech**: Remotion compositions, Kling/Veo video generation, TTS with breath-synced playback, multi-platform export\n- **Key innovation**: Breath-locked rendering — video syncs to user's breathing rhythm for therapeutic effect\n\n### Nexus Portal — 16 Dashboards\n- **What it does**: PCG quadrant visualization, real-time metrics, cross-project visibility\n- **Tech**: React + Supabase, Prefect integration\n\n### Obsidian Vault — Knowledge Persistence\n- **What it does**: Writer integration, sidecar API, knowledge page for all systems\n- **Status**: Auto-synced with Discord, KARL trajectories, memory files\n\n### X Intelligence Pipeline\n- **What it does**: Auto-analyze Twitter reposts (Discover AI channel). Extract insights using Repomix + Gemini 2.5 Flash.\n- **Size**: repo_intelligence.py (633 lines) + x_repost_watcher.py (1166 lines)\n- **Activation**: 90+ auto-spawn pane, 70+ active-tasks, 40+ silent\n- **Tools**: Firecrawl (non-GitHub URLs), Obsidian vault notes, Discord embeds\n- **Schedule**: Prefect (frequency TBD)\n\n### Milkmendelivery — CRM + Market Sweep\n- **Type**: React + Vite + Supabase CRM\n- **Location**: `~/Desktop/milkmendelivery/`\n- **Features**: 14 edge functions (market sweep pipeline), Resend email, A/B tracking\n- **Supabase project**: wovknbcvxjvayfdflxkn (RLS allows anon writes for agent CRUD)\n- **Mohamed agent_id**: c43bc216-daf5-4c1e-b0e8-d233279cc19d\n\n### Creator Shield — Anti-Bullying Service\n- **Status**: Phase 1 complete, phases 2-7 dispatched\n- **Tech**: Multi-phase anti-bullying detection, marketplace model\n- **Location**: `~/projects/creator-shield/`, Evo³ master plan at `~/Desktop/evo-cube-output/creator-shield-intelligence/`\n\n### Operation Trajectory Bloom — App Store Wave\n- **Status**: 15/17 submitted for review (WAITING_FOR_REVIEW). FeedDeck + FleetCommand pending.\n- **Wave 3**: 18/18 WAITING (submitted Mar 8 2026). Screenshots, pricing, privacy, content rights complete.\n- **What it is**: Coordinated multi-app release via testflight-automation.sh, ASC API, Xcode Cloud\n\n### Creative Shield — Vision Intelligence\n- **Type**: Vision-based content moderation, anomaly detection\n- **Status**: Early phase\n\n---\n\n## SECTION 6: CRITICAL GOTCHAS (Hard-Won Lessons)\n\n1. **~/.agents/skills/ MUST stay empty**: Gemini CLI scans both `~/.gemini/skills/` AND `~/.agents/skills/`. Duplicate skill names → fatal error. Keep skills in `~/.claude/skills/`; Gemini mirrors via symlinks in `~/.gemini/skills/`.\n\n2. **Serenity MCP build**: Source is `~/sandbox/frameworks/serenity-mcp-server/`. Rebuild with `npm run build`. Requires `GOOGLE_API_KEY` env in MCP config.\n\n3. **Claude ~/.claude.json**: Contains 14-server MCP list + auth tokens + project config. NEVER wipe. Always read first, then patch `mcpServers` key specifically.\n\n4. **NO heavy ML on Mac1**: F5-TTS, NLLB, long Whisper freeze the M2 Air (16GB). Route to Mac4/Mac5/cloud-vm.\n\n5. **exo on Mac5**: Built with `--no-deps`. Mac4 is master (:52415 API), Mac5 is worker.\n\n6. **MLX LoRA CLI (v0.29+)**: Use `python3 -m mlx_lm lora` (NOT `mlx_lm.lora`), `--num-layers` (NOT `--lora-layers`).\n\n7. **SSH to Mac4/Mac5**: Use `ssh mac5` alias (NOT IP). Config has IdentitiesOnly, multiplexing, correct key. Raw IP → \"Too many auth failures\".\n\n8. **Aura source**: `samples/CameraAccess/CameraAccess/` (NOT `Aura/` directory).\n\n9. **Pane spawn CLAUDECODE**: Unset `CLAUDECODE` before `claude --dangerously-skip-permissions` in child windows.\n\n10. **corpus-to-sft.py**: `el.get(\"text\")` returns `None` not `\"\"`. Always use `(el.get(\"text\") or \"\")`.\n\n11. **yt-dlp**: Use `/opt/homebrew/bin/yt-dlp` (v2026.03.13+), NOT pip. Requires `--cookies-from-browser safari --remote-components ejs:github`.\n\n12. **Cron state lock**: `~/.claude/state/cron_state.json.lock` — fcntl exclusive lock shared between cron_bridge.py + session_end_hook.py. Never delete while sessions running.\n\n13. **Clawdbot config**: `clawdbot.json` schema-validated; unknown keys = fatal. Use `clawdbot doctor --fix`.\n\n14. **Bun .env auto-loads**: Overrides config.toml. Check `.env` first if ports seem wrong.\n\n15. **Milkmen RLS**: `auth.role() = 'authenticated'` but anon key works for CRUD on agent_territories, profiles, locations. Use REST API directly.\n\n---\n\n## SECTION 7: SKILL ROUTING & TOOL USE\n\n### When to Use What Tool\n- **Bash**: Read-only operations (ls, git status, git diff, find). NEVER file modifications, mkdir, rm, cp, mv.\n- **Read**: Text files, code, logs, config. Use offset/limit for large files.\n- **Grep**: Full-text search on code. Supports regex, multiline matching.\n- **Glob**: Fast pattern matching for filenames.\n- **Edit**: Modify specific sections. Verify post-edit (tsc, cargo check, tests).\n- **Write**: Create new files. ONLY when no file modifications are occurring.\n- **Task/Pulse**: Long-running multi-step work. Auto-commit, heartbeat, cross-machine dispatch.\n- **Mesh/SSH**: Cross-machine injection, remote exec, terminal control.\n- **MCP tools**: Specialized skills (skills/*, browser automation, iOS, Adobe, TouchDesigner, etc.)\n\n### Sub-Agent Spawning\n**NEVER use**: `sessions_spawn` (deprecated, burns API credits, loses context)\n**ALWAYS use**: Enriched dual dispatch via `~/.clawdbot/dual-max/enriched-dual-dispatch.sh`\n- Includes AGENTS.md + SOUL.md + CLAUDE.md + relevant skills + RTD verification + quality gates\n- Routes to least-busy Claude Max account\n- Enables proper structured reporting and completion tracking\n\n---\n\n## SECTION 8: TRAINING DATA SUMMARY (5000-word limit REACHED)\n\n**Final checklist for model training**:\n- ✓ Infrastructure: 5 machines, 14+ services, credential architecture\n- ✓ Architecture: V6 twin scaffolding, KARL reward signals, Cortex behavior learning, micro-service splits\n- ✓ Decisions: Why mesh (parallelism), why V6 (model capacity mismatch), why KARL (skill accuracy), why no-pause (bandwidth bottleneck)\n- ✓ Rules: Anti-patterns, edit verification, design aesthetics, voice/tone guard\n- ✓ Personality: Direct, opinionated, proactive, momentum-driven, self-recovering, group-chat smart\n- ✓ Projects: 30+ active systems, each with clear purpose/status/tech stack\n- ✓ Gotchas: 15 hard-won lessons from production deployments\n- ✓ Skill routing: Tool selection heuristics, sub-agent patterns\n\n